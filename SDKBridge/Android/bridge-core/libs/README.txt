此目录放置 Unity 的 classes.jar 文件。

获取方式：
从 Unity 安装目录复制：
  {Unity安装路径}/Editor/Data/PlaybackEngines/AndroidPlayer/Variations/mono/Release/Classes/classes.jar

重命名为 unity-classes.jar 放入此目录即可。

注意：此文件仅用于编译时引用（compileOnly），不会打包进最终的 aar 中。
