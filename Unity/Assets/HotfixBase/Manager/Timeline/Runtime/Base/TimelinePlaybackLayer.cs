namespace Ux
{
    /// <summary>Timeline 运行时分层优先级。</summary>
    public enum TimelinePlaybackLayer
    {
        Base = 0,
        Action = 100,
        Reaction = 200,
        Additive = 300
    }
}
