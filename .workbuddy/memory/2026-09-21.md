# 2026-09-21

## 讲解：StateMachine.SetAction 的这对三元为何"看着每帧都在报 ActionStarted"

用户问 `CombatController.cs:90`：

```csharp
StateMachine.SetAction(ActionRunner.HasAction ? ActionState.Executing : ActionState.Free,
                       ActionRunner.HasAction ? StateChangeReason.ActionStarted : StateChangeReason.ActionEnded);
```

**他的判断是对的**：正因为 `ChangeState` 里 `StateId == stateId` 直接 `return false`，这对三元每帧求值才安全 —— 只有 Free↔Executing 的**边沿帧**真正发事件（f1 发 `ActionStarted`，f2–f6 静默，f7 发 `ActionEnded`）。

**但他的观感也是对的，这段代码确实"读起来像每帧都要报"。** 三条原因：

1. `ActionRunner.HasAction` 读了两遍，两个三元必须永远同步，语法上看不出这层约束；
2. 参数名叫 `reason`，读者自然按"这次操作的原因"读（= 帧），而它实际是"**这条边**的原因"；
3. 去重在**被调用方**、在 12 行之外的 `ChangeState` 里，调用点完全看不到这个前提。

**一个更深的事实（新查到）**：`ActionStarted` 甚至不表示"起了新动作"。`TryCancel`（`CombatActionRunner.cs:578-606`）是 `EndCurrent(Cancelled)` + `Start(...)` 在同一个 Tick 里连着做的，结束时 `HasAction` 又为 true → 第 3 步读到 Executing→Executing → **无事件**。所以"取消接取消"（连招）那一次起手在状态机上是静默的。它能跑对，是因为去重按**状态值**做、不按 reason 做。

**给用户的两个改法（未执行，等他拍板）**：

- 方案 A（最小）：抽 `var hasAction = ActionRunner.HasAction;`，两个三元共用。收益：一致性从推理变成语法保证。
- 方案 B（推荐）：给 `CombatStateMachine` 加镜像方法，reason 在状态机内部派生：
  ```csharp
  public bool SyncAction(bool hasAction) =>
      ChangeState(StateLayer.Action,
          hasAction ? ActionState.Executing.ToId() : ActionState.Free.ToId(),
          hasAction ? StateChangeReason.ActionStarted : StateChangeReason.ActionEnded);
  ```
  调用点变成 `StateMachine.SyncAction(ActionRunner.HasAction);` —— 调用点不再出现 reason，误解消失；错配（如 `Executing` + `ActionEnded`）从语法上写不出来。代价是新增一个 public API；`SetAction` 仍留给另外 4 处显式调用（`CombatController.cs:132`/`:148` 与 `CombatComponent.cs:271`/`:284`，全是"打断后强制 Free + ActionEnded"，同样可换成 `SyncAction(false)`，行为等价）。

## 顺带确认（用户自己改的，非本轮改动）

`CombatController` 的属性已由 `States` / `Actions` 更名为 `StateMachine` / `ActionRunner`，`CombatComponent` 调用点同步为 `Controller.StateMachine.X`。已同步更新 `MEMORY.md` 里过期的属性名。

## 执行：方案 B —— 新增 CombatStateMachine.SyncAction

用户拍板"方案B吧"。改 2 个文件，按 `unity-csharp-safe-refactor` 流程走（脚本一次做完，每条规则断言命中 1 次）。

| 文件 | 改动 |
|---|---|
| `CombatStateMachine.cs` | `SetAction` 之后新增 `SyncAction(bool hasAction)`：状态与 reason 在内部由同一条件派生；doc 写明"同状态重复调用不发事件，所以每帧调用安全" |
| `CombatController.cs:90` | 一行并列三元 → `StateMachine.SyncAction(ActionRunner.HasAction);` |

**未动**另外 4 处 `SetAction(ActionState.Free, StateChangeReason.ActionEnded)`（`CombatController.cs:132`/`:148`、`CombatComponent.cs:271`/`:284`）—— 它们是"打断后强制归位"，显式写 reason 更贴语义，且 `SyncAction(false)` 会暗示"从 HasAction 派生"而实际是强行设值。

**验证**：

- 命中数：`add-syncaction` 1×，`use-syncaction` 1×；行数 SM 212 → 224（+12 = 插入块行数），CT 211 → 211（1 行换 1 行，chars -132）
- 行尾：SM `crlf=224 lf=224 bare_lf=0`，CT `crlf=211 lf=211 bare_lf=0`
- 残留扫描：CT 里 `SetAction(` 只剩 `:132` / `:148` 两处预期内；全 `Assets` 的 `SyncAction` 只有定义 + 1 调用（另有 `CombatEditorUtility.SyncActionDurationToTimeline` 是同名前缀的编辑器工具，无冲突）
- `COMBAT_DESIGN.md` 不含 `SetAction` / `ActionStarted`，无需同步文档

**坑（本轮踩到）**：校验脚本里先调了 `load()`（它会把 `\r\n` 归一成 `\n`）再统计 `crlf == lf`，结果恒为 False 的**假报警**。行尾校验必须读**原始字节**，不能读归一化后的文本 —— 已独立写 `_verify_*.py` 重查确认无误。

**待用户在 Unity 里做**：过一遍编译（沙箱不能编 C#）。热更 dll 也要重编译 —— `CombatStateMachine` 在 `Unity.HotfixBase` 里。

## 追问：取消接起手时 `SyncAction` 前后都是 `hasAction`，没有变化，正常吗

**正常，而且是设计想要的**——"变化"被记在了另一条通道上。

- `SyncAction(true)` 落在已 `Executing` 的层上 → `ChangeState` 在 `CombatStateMachine.cs:195` 的 `runtime.StateId == stateId` 处 `return false`：不发事件、**也不执行 `:201` 的 `runtime.Set(stateId, 0)`**。
- 之所以这不算丢信息：Action 层的语义是"**有没有招式在跑**"（两值），不是"**是哪一招**"。实例身份（`InstanceId` / `CurrentAsset`）从来没进过状态机，它只活在 `CombatActionRunner`。
- 同一帧 f6，`ActionChanged` 发了**两条**（`EndCurrent(Cancelled)` → `:646`，紧跟 `Start(...)` → `:633`）。所以"运行层有边、状态机层无边"——两条通道粒度不同、各自完整。
- 表现层不受影响：`CombatTimelinePlayer.cs:66` 的 owner key 是 `$"action:{actions.Current.InstanceId}"`，`Start` 里 `++_localSequence` 每次都变 → 连招强制重播动画（正是 `Start` 注释说的"表现层用它当 owner key"）。

**新查到的坑**：因为 `Set(stateId, 0)` 没执行，连招时 Action 层的 `Frame` **不重置**，会从第一次起手一路累加；而 `Current.ActionFrame` 已回到 0。现状零影响（该层 `Frame` 无生产消费者），但将来若照另外三层接到播放头上就会出问题。已记入 `MEMORY.md`。

**没动代码**：修这个要么把 `InstanceId` 引进状态机（破坏"只有两个值"），要么删掉 Action 层（它是快照与 `IsDefined` 校验的参与者）。收益为零，留观察。

## 追问：哪个选项是"完整修复"（不要补丁、不要以后再说）

**结论：只有「删掉 `StateLayer.Action` 整轴」是根除，其余都是补丁。** 依据是这次补查到的四条事实：

1. **哈希里它本身就是重复的**：`BattleWorld.cs:367` 打进的是 Action 层状态 id，而紧挨着的 `:372` 打进的是 `ActionRunner.HasAction` —— 同一个 bit 的两种编码。且 `HasAction => CurrentAsset != null`（`CombatActionRunner.cs:111`）、`EndCurrent` 里 `Current = default; CurrentAsset = null`，两者永远同步。删掉零覆盖度损失。`ComputeStateHash` 全仓库只被测试调用，**无落盘、无网络传输**，改哈希组合不破坏任何契约。
2. **快照里也是重复的**：`UnitCombatSnapshot`（`CombatActionRunner.cs:36-42`）独立存了 `HasAction` / `Action` / `AcceptedHits` / `LocalActionSequence`。所以 `CombatStateMachineSnapshot.Action` 是第二份。
3. **表现层早已拒绝它**：`CombatStateId.IsStatePresentationMappable` 对 Action 直接 `return false`（`CombatState.cs:134`）；编辑器 `PresentationLayers`（`CombatEditorWindow.cs:99-103`）也不含它；且有专门测试守着"整层禁止"。
4. **生产代码零读者**：`StateMachine.Action` / `GetStateFrame(StateLayer.Action)` 的消费者只有测试。
5. 附加证据：`StateLayer` 自己的文档注释写着「具体攻击和技能**不作为状态节点**，而由 CombatActionRunner 管理」（`CombatState.cs:6`）—— `Action = 1` 与该注释直接矛盾。

**为什么"实例变了就重置 Frame"（上一轮我给的选项 2）是补丁**：`Confirm()`（`CombatActionRunner.cs:274-298`）会用权威值**覆盖 `InstanceId`** 并把 `ActionFrame` **重算成跳变值**（`_simulationFrame - StartSimulationFrame`）。只做"实例变了归零"的话，两条时间线在 Confirm 路径上照样分叉 —— 补丁只是把窗口收窄，没消除。而且它会给 Action 层的 `Frame` 一个与其余三层**不同的含义**，等于新增一处不一致。

**删轴必须一起处理的两处真风险**：

- **编辑器 5 处 `enumValueIndex` 转型**（`CombatEditorWindow.cs:1016 / 1061 / 1065 / 1118 / 1505`）：`enumValueIndex` 是**枚举名列表里的下标**，不是枚举值 —— 现在它能当值用纯属巧合（值恰好连续从 0 起）。若删掉 `Action` 后**保留数值空洞**（`Control=2 / Life=3`），这 5 处会把 Control 读成 1，UI 直接错。必须改成 `intValue`。
- **`StateLayer` 的序列化值**：现存 profile 资产（`Data/Res/Combat/HeroZS/HeroZSCombatProfile.asset`）只有 `layer: 0` 两处，无 1/2/3。所以"保留空洞"与"连续重排"都安全，但含义不同 —— 空洞方案零重解释、残留 `layer: 1` 会校验报错；重排方案编辑器零改动，但残留 `layer: 2` 会被静默解读成 `Life`。

**连带会变成"无人写入"的枚举值**：`StateChangeReason.ActionStarted` / `ActionEnded`（它们是 `SetAction`/`SyncAction` 专用），随之删掉。`ActionState` 枚举本身也删。

**保留**：`CombatActionRunner.ActionChanged` + `CombatActionEndReason` —— 那是"按动作实例"那条通道，与本轴无关。

**已向用户确认中（未动手）**。

## 产出：删轴提案文档（用户要求先交给其他 agent 审核）

用户决定"先不改代码"，要求我把这个问题总结成可供第三方审核的材料 → 已写 **`.workbuddy/review/StateLayer-Action-removal-proposal.md`**。

文档定位：**自包含**（外部 agent 不需读对话历史即可审），共 12 节 —— 结论 / 触发问题 / 现状事实（逐条带 `文件:行`）/ 三条独立重复性证据 / 逐文件改动清单（12 项）/ 为什么"实例变了归零"是补丁 / 编辑器 `enumValueIndex` 必改点 / 枚举值空洞 vs 连续重排的选择 / 明确不在范围的项 / **请重点质疑的 6 点（含让审核方独立复核 grep 面、`HasAction` 与 Action 层是否真等价、`enumValueIndex` 是否有第 6 处漏掉）** / 验证清单（沙箱不能编译 C#）/ 附判断标准。

**本轮新增核对到的事实**（此前未记）：

- `CombatActionRunner.Start` 定义在 `:615`，`InstanceId = ++_localSequence` 在 `:622`（文档里已用准行号）。
- `ComputeStateHash()` 全仓库**只有定义，无任何非测试调用方** —— 复核过，哈希改动不破坏对外契约。
- `UnitCombatSnapshot` / `BattleWorldSnapshot` **无任何落盘或网络序列化**（无 `JsonUtility` 等调用点），快照只在内存里滚。
- `HeroZSCombatProfile.asset` 全文件只有两处 `layer: 0`（`:21` / `:28`），无 1/2/3。
- 全仓 `StateLayer.Action` / `ActionState.` 引用面已逐条列进文档表格，生产代码**零读者**（只有测试读 `StateMachine.Action` 与 `GetStateFrame(StateLayer.Action)`）。

**代码依旧一行未动**：本轮只做只读核查 + 写文档。工作区 `StateLayer.Action` 整轴完好。

## 外部审核回来后的修订（第二轮）

用户把提案文档交给另一个 agent 审核，带回了 7 条修正意见。我的处理：**6 条采纳、1 条反向修正**。

**采纳并已改文档**：

1. **"永远同步"是过强表述** —— 实测 `CombatActionRunner.StartAction`（`:258`）是 **public**，测试里 10 处直接调、生产 0 处，绕过 `SyncAction`；`CombatController.RestoreSnapshot`（`:174-199`）又是两次独立恢复、无交叉校验。所以一致性是**约定**不是**不变量** → §2.2 重写（加了三条绕过路径的表格）。这条反而**加强**了删轴的理由。
2. 哈希里的"重复"要加前提 → §3.1 重写，并补上"分叉检测能力目前是理论上的"（`ComputeStateHash` 无生产调用方）。
3. 快照里说"完全重复"不准（`CombatStateMachineSnapshot.Action` 还带一个独立 `StateFrame`）→ §3.2 改成对照表。注：初稿 §5.2 已经写过"两个 Frame 概念不同且会分叉"，这次是收紧 §3.2 的写法。
4. "只有删轴才是根除"措辞过强 → 改为"当前架构下推荐的最小复杂度方案"，新增 §5.3 四个候选方案对比表（B 让 Action 层跟踪 `InstanceId` 是**更差**的设计；D 收口入口是**补充**而非替代）。
5. **`enumValueIndex` 计数我写错了**：正文写"5 处"却列了 6 个位置，还漏了 `CombatTestProfiles.cs:57` —— 实测应为 **7 处**（`CombatEditorWindow` 6 + `CombatTestProfiles` 1）。已在 §4 表 / §6 / §9 全部改正。
6. "工作区无半成品改动"不成立（存在未提交的 `UnitStateMachine → CombatStateMachine` 重命名等）→ 新增 §0「代码基线」小节列出全部未提交项，并要求先固化独立基线提交。

**反向修正 1 条**：审核意见说"采用空洞方案后 `LayerIndex()` 不能再把枚举值当数组下标、必须改显式映射" —— **事实有误**，`CombatStateMachine.LayerIndex`（`:211-221`）**本来就是显式 switch**。已把这个更正写进 §7 末段。

**审核补充的影响面（量级比它暗示的小）**：`BuildStableId`（`CombatStatePresentation.cs:85-88`）把 `(int)layer` 写进 **StableId**（持久化字段），`CombatTimelinePlayer.cs:48/55/69` 的 owner key 也用 `(int)StateLayer`。但实测现存 StableId 只有 `state.0.1.default` / `state.0.2.default`，全仓 `state.[123].` **零命中** → 两方案对已持久化数据都无影响。已列入 §7 并注明量级。

文档新增 **§9.1 审核回执**（逐条列采纳/未采纳 + 理由），让下一轮审核方能看到哪些点已经处理过、哪些被反证。

## 给出修复方案推荐（用户问"你推荐用哪个方案修复"）

**推荐：A 删掉 `StateLayer.Action` 整轴 + §7 选 A（保留数值空洞）**，并把 D（入口收口）**降级为后续独立项、不作为前置**。已写进文档（新增 §0「推荐的两个决定」表 + §5.4 + §7 推荐理由 + §13 后续项）。

三条取舍依据：

1. **C 是定义上的半程** —— 只解决 §5.2（帧分叉），留下 §2.2 的三条绕过入口与重复建模，不符合用户"要么完整修复、要么明确保留"的口径。
2. **D 不该当前置**：D 要守的不变量正是 A 要删的东西（先加固一个即将被删的对象）；且 A 会让 D 的入口从 4 条缩到 2 条 —— `SetControl`/`SetLife`/`CombatComponent` 那 4 行 `SetAction(Free, ActionEnded)` 随轴消失。补查确认 **`Reject`/`Confirm` 从不绕过镜像**（只改 `Current`/`CurrentAsset`，含 `:293` 的 `EndCurrent(Completed)`），所以 D 的残余只有 `StartAction`（public，10 处测试调用、生产 0 处）与 `RestoreSnapshot`。
3. **枚举选空洞（A）而非连续重排（B）**，新增一条此前没写进文档的理由：**"现存资产只有 `layer: 0`"这个结论只对本工作区成立** —— 其它分支 / 工作副本 / 同事的资产看不到，A 对未知数据一律报错、B 是猜一个语义。另加两条自我反驳也写进文档了：空洞可能被后人"顺手补上"（靠注释挡）；A 的 7 处代码必须跟着改，改错是**假通过**而非报错（`CombatTestProfiles.cs:57` 最隐蔽）。

**顺带核到的新事实（本轮）**：

- `CombatActionRunner.Restore`（`:435`，签名 `(in CombatActionSnapshot, bool hasAction, long simulationFrame, CombatAcceptedHitSnapshot[] = null, long localActionSequence = -1)`）**已有一半守卫** —— `:440` 在 `hasAction && snapshot.InstanceId <= 0` 时抛异常；`Confirm`（`:274`）开头即 `authoritativeInstanceId <= 0` 返回 false。所以"SnapshotId 与 hasAction 不一致"唯一未守的方向是 `hasAction == false` 携带 `InstanceId > 0`（`:447-454` 直接 return，静默忽略快照动作字段）。
- 因此 `HasAction` 与 `Current.InstanceId > 0` 在当前所有路径下等价（`Start` 里 `++_localSequence` 从 0 起 → 实例 id ≥ 1；`EndCurrent` 里 `Current = default`）→ **可以改为派生并删掉快照字段**，属于 §13.1 后续项，不并入本次删轴（代价：`UnitCombatSnapshot.CurrentVersion` 2 → 3）。

**代码依旧一行未动**：本轮只读核查 + 改文档。



## 审核其他 AI 的 `StateLayer.Action` 删轴落地（2026-09-21 下午）

工作区已由其他 AI 完成删轴（不是提案状态了）。**代码基线**：`UnitStateMachine→CombatStateMachine` 改名 + Controller 属性改名（`States`/`Actions` → `StateMachine`/`ActionRunner`）+ `CombatComponent` 转发门删除 + 删 Action 轴，**全部仍未提交**（HEAD 是 `9947ea6b 重构技能模块`）。

**结论：功能正确、删除完整、已通过 Unity 编译。4 处需修。**

**做对的**：19 个文件删除全对应；全仓源码 `StateLayer.Action|ActionState|SyncAction|ActionStarted|ActionEnded|UnitStateMachine` 零残留；`CurrentVersion` 2→3 且 doc 注释记了版本历史；测试同步得体（`CombatRuntimeTests` 删 3 处重复断言、`CombatEditorWorkflowTests` 删 2 断言 + 整个 `StatePresentationValidationUsesTimelineOrProfileContext` 用例）。

**编译实证（本模块第一次拿到）**：`Library/ScriptAssemblies/` 三个程序集 14:35:43/44/45 生成，源码最晚 14:34:03 → **0 个源文件比产物新**；`Unity/Logs/AssetImportWorker0.log`（14:35:54）`error CS` 出现 **0 次**。

**需修的 4 处**：
1. **P1 删掉了最该留的注释** —— `CombatActionRunner.TryCancel` 的整个 doc 被删，含 `⚠ 这正是"狂点打不出伤害"的成因：普攻 1001 的取消窗口是 [5,26) 且指向自己，帧 5-26 之间每次按键都会把动作重置回帧 0，永远走不到帧 41 的命中窗口。` 这是"易踩的坑"（用户明确要留的两类之一）；替换进去的是 `//这里要看取消窗口的目标动作是否和命令的动作一致，并且当前动作是否在取消窗口内`（把 if 条件翻译了一遍 = 纯噪声）。**方向反了**。同类：`AdvanceActionFrame` 的 `// 播完了自动收尾。这里的 DurationFrames 就是资产里的 70。` 被删。
2. **P2 3 处行尾空白**（`git diff --check` 客观确认）：`CombatActionRunner.cs:248`（`{` 后 16 空格）、`:550`（整行只有空格，原本是一句注释）、`:589`。
3. **P3 步骤号/属性名失效**：`CombatController.cs:74` doc 写"原因见**第 4 步**"，但删掉 SyncAction 后 Locomotion 是第 3 步（`:89`）；`CombatController.cs:100` 仍写 `Actions.BlocksMovement`（现为 `ActionRunner.BlocksMovement`）；`COMBAT_DESIGN.md:168` 仍写 `States.AdvanceTo` + `Actions.Tick`。
4. **P4 删测试时连带丢了 `issue.Context` 覆盖** —— 被删用例的 `Assert.AreSame(timeline/profile, issue.Context)` 是全仓**唯一**覆盖 `CombatValidationIssue.Context` 的断言（grep `Assets/Editor/Combat/Tests` 已无任何 `.Context`）。应**改用另一个非法层**而非删除：`StateLayer.Control + ControlState.Normal` 走同一校验分支。

**我自己的误判（已撤回）**：先报"9 个 .cs 从 CRLF 变 LF 是回归"，实为假警报 —— 见 `MEMORY.md` 工具坑里那条新写的自检方法。

**顺带核到**：`ProjectSettings/ProjectVersion.txt` = **`6000.4.3f1`**（不是 2021.3.48f1）；`Assets/Data/Res/Code/Unity.HotfixBase.dll.bytes` + `Assembly-CSharp.dll.bytes` 时间是 **2026-01-08**，比本次改动早 8 个月，走热更会与代码对不上。

**代码依旧一行未动**：本轮只读核查。

## 讲解：删掉 StateLayer.Action 之后现在的流程

用户问"没有了 StateLayer.Action 层，那现是什么流程"。核对当前落盘代码后确认：

- `StateLayer` 现在是 3 层：`Locomotion=0 / Control=1 / Life=2`。
- `CombatController.Tick` 从 4 步降到 **3 步**：① `StateMachine.AdvanceTo` → ② `ActionRunner.Tick` → ③ Locomotion 三选一。删掉的是原来夹在 ②③ 之间的 `StateMachine.SyncAction(...)` 镜像步。
- **动作影响状态机的唯一路径**：`ActionRunner.BlocksMovement` → `CombatController.IsMovementBlocked` → 第 ③ 步把 Locomotion 压成 Idle。所以动作起手现在至多引发**一条**状态机边（`Locomotion: Move→Idle`，且仅当该动作 `movementPolicy=Block` 且当时在走）。第 ② 步早于第 ③ 步的顺序不变量未变。
- 快照：`CombatStateMachineSnapshot` 少了 `Action` 层，但 `UnitCombatSnapshot.Action` / `HasAction` 仍在 —— 动作的持久化没缺。`CurrentVersion` 2 → 3。
- 哈希（`BattleWorld.cs:366-380`）：三层状态 id + `ActionRunner` 的 `HasAction/ActionId/ActionFrame/InstanceId/StartSimulationFrame/...`。`:371` 的 `HasAction` 与 `:372/:374` 的动作字段仍是可派生冗余。
- 之前那条「连招时 Action 层 `Frame` 不归零、与 `Current.ActionFrame` 分叉」的坑**随轴消失**（载体没了）。

**上一轮审核报的问题**：P3（过期注释）本轮已修，见下一节；**P1**（`CombatActionRunner.TryCancel` 的 doc 被删，丢了"狂点打不出伤害"那条说明）、**P2**（三处行尾空白）、**P4**（被删的 `StatePresentationValidationUsesTimelineOrProfileContext` 应改成另一个非法层而不是删掉）—— 这三项仍未修。

## 修正：过期注释与文档悬空指代（用户："注释过期你改一下"）

按上轮审核的 P3 修，同时扫出同类漏网的 4 处 —— 共 **7 处替换，4 个文件**：

| 位置 | 改前 | 改后 |
|---|---|---|
| `CombatController.cs:74` | 原因见**第 4 步** | 原因见**第 3 步** |
| `CombatController.cs:100` | 含 `Actions.BlocksMovement` | 含 `ActionRunner.BlocksMovement` |
| `CombatComponent.cs:160` | IsMovementBlocked 读的就是**动作层**的状态 | 读的就是 `ActionRunner.BlocksMovement` |
| `CombatState.cs:95` | 因 `ResolveTimelineOwner` 的层级优先级 | 因 `CombatTimelineResolver.Resolve` 的层级优先级 |
| `COMBAT_DESIGN.md:20` | `CombatActionRunner.cs:175` / `:202`；`CombatController.cs:109` | `:275` / `:301`；`:149` |
| `COMBAT_DESIGN.md:22` | `CombatComponent.cs:243` `ResolveTimelineOwner`；Life > Control > **Action** > Locomotion | `CombatTimelinePlayer.cs:36` `CombatTimelineResolver.Resolve`；Life > Control > Locomotion，命中时独占并停掉 Action 层 |
| `COMBAT_DESIGN.md:168` | `States.AdvanceTo` + `Actions.Tick` | `StateMachine.AdvanceTo` + `ActionRunner.Tick` |

**本轮新发现**（上轮 P3 只报了 3 处）：`CombatComponent.cs:160`、`CombatState.cs:95`、`COMBAT_DESIGN.md:20/22`。共同成因是"注释/文档里引用了已改名或已不存在的符号" —— `ResolveTimelineOwner` 现在叫 `CombatTimelineResolver.Resolve`（在 `CombatTimelinePlayer.cs`），`UnitStateMachine` → `CombatStateMachine` 改名后文档里的行号引用跟着漂。

**方法**：字节级替换（old/new 都不含换行），每条断言命中恰为 1 次，替换前后逐文件比对 `crlf`/`lf` 计数 —— 4 个文件全部 `EOL-UNCHANGED`（`CombatState.cs` 本来就是纯 LF，其余三个 CRLF）。

**顺带**：`COMBAT_DESIGN.md:144` 目录树的 `CombatStateMachine` 比旧名长 2 字符，描述列歪了 2 格，已对齐。

**最终残留扫描**（`Assets/` 下全部 `.cs` + `.md`，排除 ThirdParty）：`UnitStateMachine` / `ResolveTimelineOwner` / `StateLayer.Action` / `ActionState.` / `ActionStarted` / `ActionEnded` / `第 4 步` **全部 0 命中**。仅剩 3 个无关命中：`SyncActionDurationToTimeline`（编辑器工具，同名前缀）、`InputActionState`（Unity InputSystem）、`CombatComponent.cs:158` 的"四步"（指 `TickLogic` 内部 4 步，正确）。

**代码逻辑一行未改**，纯注释/文档。改的是热更层（`Unity.HotfixBase` + `Assembly-CSharp`），但注释不进 IL，**本次不需要重编译 dll**。

## 收尾：P1 / P2 / P4 一并修掉（用户："一并处理吧"）

上轮审核留的 3 项全部处理，另**追加**修了 4 处文档行号失效 + 1 处措辞歧义。改动共 5 个文件。

| 项 | 处理 | 落点 |
|---|---|---|
| P1 | 恢复 `TryCancel` 的 ⚠ 坑注释（压缩成 2 行）；删掉复述 `if` 的注释、复述 `for` 的注释 | `CombatActionRunner.cs:578-584` |
| P1 | `AdvanceTo` 那行空白**直接删**（原句与方法自身 doc 重复，不是"唯一线索"） | `CombatActionRunner.cs:549` |
| P2 | 清 3 处行尾空白/空白行，并把 `}` 与 `foreach` 之间被误删的**空行补回** | `CombatActionRunner.cs:248 / 549 / 592` |
| P2 | 清 4 处行尾空白，补回 `ActionRunner.Tick` 与 `if (!_grounded)` 之间的空行 | `CombatController.cs:84-100` |
| P3 | doc 里"原因见第 3 步"改为**直述原因**（步骤编号随重写一起消失，编号引用已彻底失效） | `CombatController.cs:74-75` |
| P4 | 恢复 `StatePresentationValidationUsesTimelineOrProfileContext`：`Action/Executing` → `Control/Normal`，消息过滤 `"Action 层"` → `"不是允许配置表现映射"` | `CombatEditorWorkflowTests.cs:551-600` |
| 追加 | 文档行号全量核验（脚本抽 `` `X.cs:NNN` `` + token 逐一验），修 4 处失效 | `COMBAT_DESIGN.md:20 / 82 / 231 / 407` |
| 追加 | 「停掉 Action 层」→「停掉 Action **播放**层」（`StateLayer.Action` 已删，原文与 `TimelinePlaybackLayer.Action` 同名易混） | `COMBAT_DESIGN.md:22` |

**新增工具方法（值得复用）**：文档行号引用核验脚本 —— 用正则抽 `` `([\w/]+\.cs):(\d+)`\s*`([^`]+)` ``，按 basename 建索引找真实文件，检查该 token 是否真的出现在该行；不匹配就 grep 出真实行号。这次一次抓出 4 处漂移（`149→138`、`76→131`、`212→16`、`285→517`），全是改名前后的历史欠账。

**验证结果**：

| 检查 | 结果 |
|---|---|
| `git diff --check` | 空输出（前一轮有 7 条 trailing whitespace） |
| 4 个改动文件行尾 | 全 CRLF，`CRLF == LF`（逐文件原始字节统计） |
| `StateLayer.Action` / `ActionState` / `SyncAction` / `UnitStateMachine` 全仓 | 0 命中 |
| `profile.FrameRate` / `TimelineAsset.SetFrameRate` / `CombatValidationIssue.Context` | 签名均已存在，恢复的测试调用合法 |
| 编译 | **未验证**（本环境不能编 C#） |

**本轮踩的坑（新）**：`open(path, encoding="utf-8").read()` 会把 `\r\n` 归一成 `\n`，于是"逐行改写再写回"这类脚本里 `crlf == lf` 断言**必然失败**。要保留行尾必须 `open(path, 'rb')` 后 `decode`。第一次跑补丁时正是这样 —— 前两个 `.cs` 文件已落盘、第三个 `.md` 断言炸掉，结果脚本半途中断（.cs 成功、.md/测试未跑）。**补丁脚本要按文件分段、可单独重跑**，否则重跑整脚本会按旧行号二次改坏已改过的文件。

**刻意未动的一项**：`CombatActionRunner.Tick` 里还剩 `// 0.`（`:237`）与 `// 1.`（`:242`）两个序号注释，而原 `2.` / `3.` 已被改成无序号散文 —— 序号体系只剩一半。属排版一致性，用户偏好最小侵入，先留着并已在审核文档 §6 记明。

## 讲解：删掉 Action 层之后的优缺点（用户提问，无代码改动）

用户问"少了 Action 这个层，有什么优点和缺点"。

**定位**：删掉的不是"一层逻辑"，而是一份与 `CombatActionRunner.HasAction` 完全重复的存储（bool 投影成 int 状态）。所以好处全部来自"去掉副本"，代价全部来自"少一个统一读/写入口"。

**优点**（收益递减）：① 唯一真相源，不存在"状态机说 Executing、Runner 说没动作"；② 消除每帧同步点（`SyncAction` 是顺序敏感的追同步，正是 Frame fork 的土壤）；③ 消除粒度损失（Action 层只有 2 值，Runner 有 `ActionFrame`/`InstanceId`/`HasHitConfirmed`）；④ 快照只剩一份，回滚不会撕裂；⑤ `Layers`/`_layers`/`LayerIndex`/`CaptureSnapshot`/`RestoreSnapshot`/`GetLayer` 六处同步表各少一项（这几处漏改不报编译错）；⑥ 每帧常数开销下降；⑦ 概念边界清楚（状态层答"宏观处境"，Runner 答"在不在出招"）。

**缺点**：① 失去统一事件面 —— `StateChanged` 不再覆盖动作起停（**但 `CombatStateMachine.StateChanged` 与 `CombatActionRunner.ActionChanged` 当前全项目零订阅**，属结构缺口不是回归）；② 失去统一查询面 —— `GetCurrentStateId(StateLayer)` / `GetStateFrame` / `IsPlaying` / `CaptureSnapshot` 不再覆盖动作；③ **枚举/快照格式变更：`StateLayer` 从 4 值变 3 值且是连续重编号（0/1/2）。当前快照无落盘、无网络传输，所以安全；一旦引入网络同步或落盘，必须先冻结枚举值**（否则旧的 `Action=0` 会被读成 `Locomotion`）；④ 表现层少一个状态映射源（`Action/Executing` 不能再配表现，动作表现只能走 `profile.GetActionTimeline`）；⑤ **命名坑：`TimelinePlaybackLayer.Action`（表现层，值 100）仍在**，与已删的 `StateLayer.Action` 同名不同域，读文档易混；⑥ 若将来动作需要多子状态，不能再往 Action 层加值（应由 `CombatActionRunner` 结构承担）。

**顺带澄清**：combo-cancel（同帧 `EndCurrent(Cancelled)` + `Start`）在删除前 Action 层是 `Executing→Executing` 被 `ChangeState` 去重 → 静默；Runner 层却发两条 `ActionChanged`。"表面无变化、实际换了动作"的伪一致性随删轴消失，语义更诚实。

**代码零改动**，本轮只读核查 + 讲解。

## 更正：「表现层少一个状态映射源」这条缺点不成立（用户追问"不能再配表现啥意思"）

上一节我把缺点 ④ 写成"`Action/Executing` 不能再配表现"，措辞不准确。**查了 `HEAD` 版本（= 删轴前，未提交改动都在工作区）后确认：Action 层在删轴之前就已经被显式禁止配表现了，这条是名义代价，零能力损失。**

**"配表现"的机制**：资产 `CharacterCombatProfile.statePresentations` 是一张 `(StateLayer layer, int stateId, string variantId) → TimelineAsset` 的映射表（`CombatStatePresentation.cs:14`）。编辑器入口是 `CombatEditorWindow.AddNextDefaultPresentation`（`:1436`），它遍历 `PresentationLayers` × `CombatStateId.GetMappableStateIds(layer)` 找出还没配的组合，插一条新条目。运行时由 `CombatTimelineResolver.Resolve` 调 `profile.GetStatePresentation(layer, stateId, variant)` 取出，放进 `CombatTimelinePlan.Base`，播在 `TimelinePlaybackLayer.Base`。

**删轴前的三道关卡（全部来自 `HEAD` 版本实测）**：

1. `CombatStateId.IsStatePresentationMappable`（HEAD `CombatState.cs:126-131`）**第一句就是** `if (layer == StateLayer.Action) { return false; }`；doc 注释原文：「只排除结构性错误项：- Action 层整层禁止（由技能列表管理，ValidateRuntime 会拒绝）；- Control.Normal、Life.Alive……」→ 删轴时这 4 行随枚举一起删掉。
2. `CombatEditorWindow.PresentationLayers`（HEAD `:94-99`）**删轴前就只有 3 项** `Locomotion / Control / Life`，从来不含 Action。
3. `CharacterCombatProfile.ValidateRuntime`（`:273-279`）对非法映射直接 `throw new InvalidOperationException($"状态表现不能映射该状态: ...")`；编辑器侧 `CombatEditorUtility`（`:655-664`）报 `CombatValidationIssue(Error, "...不是允许配置表现映射的逻辑状态。")`。

**动作表现走的是完全独立的第二张表**：`CombatActionPresentation`（`CombatActionPresentation.cs:11`）= `(CombatActionAsset action, TimelineAsset timeline)`，存在 `actionPresentations` 里，键是 `actionId`，与 `StateLayer` 无关。`CombatTimelineResolver.Resolve`（`CombatTimelinePlayer.cs:61-68`）在 `actions.HasAction` 时用 `profile.GetActionTimeline(actions.Current.ActionId)` 取出，放进 `CombatTimelinePlan.Action`，播在 `TimelinePlaybackLayer.Action`。

**结论修正**：删轴在这条上实际是**减法** —— 少了一个"专门用来否决整个枚举值"的分支（`IsStatePresentationMappable` 从 3 个否决条件降到 2 个）。上一节的"缺点 ④"应改写为：「`statePresentations` 的候选层从 4 个枚举值降到 3 个；但 Action 层在被删前就已由三道校验禁止配置，属零能力损失。」

**教训**：判断"某个枚举值/能力是否真被使用"时，不能只看"它是否存在于类型里"，必须查**它是否被显式的允许列表/校验条件放行**。这次的 `PresentationLayers` 与 `IsStatePresentationMappable` 就是两道"存在但被否决"的例子 —— 用 `git show HEAD:<path>` 取改动前版本可直接对比，比翻对话历史可靠。

## 讲解：删轴的代价重新分级 + "什么时候真的需要新状态轴"（用户追问）

用户问两件事：①"删除后反而是优点？" ②"若将来动作需要多子状态，什么情况会需要"。

### 代价重新分级（修正上一节的列表）

| 代价 | 性质 | 说明 |
|---|---|---|
| 表现映射源少一个 | **假代价** | 见上一节更正：删轴前三道校验已禁止 Action 层配表现 |
| 未来多子状态 | **极弱** | 见下节判据，四条同时成立才需要，且那时几乎必落 `Control` |
| 枚举值冻结 | **阶段性免费** | 关键发现：这条不是"删轴"带来的，而是"一旦落盘/联网就冻结任何枚举"的通用约束。**删轴发生在网络同步之前，所以现在删是免费的**；若先联网再删，才需要数据迁移 |
| 统一事件面 / 查询面 | 真实，但当前零消费者 | `StateChanged` 与 `ActionChanged` 全项目零订阅 |
| `TimelinePlaybackLayer.Action` 同名不同域 | 真实且持续 | 纯阅读成本，不随代码演进消失 |

**结论**：不能笼统说"删除反而是优点"。准确表述是**在本项目当前时点删轴的净成本几乎为零**；收益来自"去掉副本"（中性减法），不是"删本身产生好处"。

### "什么情况会需要新状态轴"——四条判据

往 `StateLayer` 加值（无论叫不叫 Action）需要**同时**满足：

1. **对所有动作通用** —— 不是某一招特有
2. **需要外部能 set** —— 不只是生产方自己推进
3. **需要"进入/离开"的边沿语义** —— 而不是连续变化的数值
4. **与其它状态互斥** —— 同一时刻只能处其一

只满足部分的情况都有现成去处（本轮已核）：

- 只对某一招成立 → `CombatActionAsset` 字段（现有 `durationFrames` / `movementPolicy` / `cancelWindows` / `hitWindows` / `damage` / `appliedBuff`，`:121-146`）
- 只是招式内进度 → 从 `ActionFrame` 派生
- 语义是异常/控制 → 归 `Control` 层（`ControlState` = Normal / Stunned / Knockback / Frozen，`CombatController.SetControl` 是 public 外部入口）—— **`Control` 层本身就是"外部可 set + 互斥 + 有边沿"这一类，所以新出现的同类中间态会先落这里，而不是回动作轴**
- 只是跨单位查询 → 直接读 `entity.Controller.ActionRunner.HasAction`（`BattleWorld.cs:371` 已这么做，无需进状态层）

真正会触发新轴的需求形态：**需要在"没有任何招式实例存在"时依然成立的、不属于移动/控制的通用动作处境**（例如互斥的"戒备姿态 / 锁定姿态"这类，且各自要独立 Timeline）。当前跑不出这类需求。

### 一个重要澄清

**"不能再往 Action 层加值" ≠ "不能再加新状态轴"。** `StateLayer` 现在是 `0/1/2`，将来直接追加一个 `= 3` 即可 —— 不需要重编号、不影响现有三个值。删掉的是**那个特定的镜像轴**（把 `ActionRunner.HasAction` 这个 bool 投影成状态），不是"加轴"的能力。

**代码依旧零改动**，本轮只读核查 + 讲解。

## 核查：`CombatController.IsGrounded` 改 auto-property（用户自己改的，问能否这样写）

用户把 `private bool _grounded = true;` + `public bool IsGrounded => _grounded;` 改成了 `public bool IsGrounded { get; private set; }`（声明处第 15 行留了 `// private bool _grounded = true;` 墓碑注释，属性**没有** `= true` 初始化器），问是否可行。

**结论：可行，功能完全等价，零行为变化 —— 但理由不是"省了变量"这么简单：`= true` 那行本来就是死代码（对外不可观测）。**

原因链：`Initialize()` 第一句就是 `Release()`，而 `Release()` 末尾写了 `IsGrounded = true`。所以在任何外部代码能读到 `IsGrounded` 之前，它已被置 true。且所有读取路径都有 `IsInitialized` 守卫：`Tick` 早退、`CaptureSnapshot` 返 null、`BattleWorld.ComputeStateHash`（`:361`）跳过未初始化实体、`SetControl`/`SetLife` 早退。测试也全部先 `Initialize`（`CombatRuntimeTests.cs:1040`）。所以"构造后未 Initialize"这个窗口没有任何读者。

**残留脆弱点（值得记）**：`IsGrounded` 的"默认着地"意图现在**只活在 `Release()` 的一行里**（原来声明处与 `Release()` 两处一致、互为印证）。若将来有人改掉 `Release()` 那行，初始值会静默变成 false，症状是 `Tick`（`:90`）第一帧就把 Locomotion 强制成 `Airborne`。建议补 `= true` 初始化器（功能等同，但把不变量留在声明处），或抽 `const bool DefaultGrounded` 让两处共用一处定义。

**另外发现的两处小尾巴**（未改，用户未要求）：`CaptureSnapshot` 里 `IsGrounded = IsGrounded,`（`:153`）现在读起来像自赋值 —— 左边是 `UnitCombatSnapshot` 的字段（定义在 `CombatActionRunner.cs:43`，public），右边是 `CombatController` 的属性，同名不同物；建议写 `this.IsGrounded` 消歧义。以及 `{get;private set;}` 的空格风格与全文件其余 5 处 `{ get; private set; }` 不一致。

**代码零改动**，本轮只读核查。

## 「私有字段 + 转发属性」统一改成自动属性（用户要求）

把 Combat 模块里 `public X Foo => _foo;` 形式的转发属性改成 `public X Foo { get; private set; }`。

**改了 8 个属性 / 5 个文件**

| 文件 | 改动 |
|---|---|
| `Runtime/Unit/AttributeSet.cs` | `IsInitialized` / `MaxHp` / `Hp`（3 个字段下线） |
| `Runtime/Core/CombatStageBuffers.cs` | `CombatFrameEventTable.Frame` / `.Count`、`CombatHitBuffer.Frame`（保留 `= -1` 初值） |
| `Runtime/Unit/CombatActionRunner.cs` | `LocalSequence` |
| `Hotfix/Common/Combat/CombatComponent.cs` | `PresentationVariant`（初值搬到属性上） |
| `Runtime/Unit/CombatController.cs` | `IsGrounded` 补 `= true`，删掉墓碑注释 `// private bool _grounded = true;` |

**没改的 2 个（转不了，不是不想改）**

`BattleWorld.OrderedEntities`（`_ordered` 是 `ICombatEntity[]`）和 `ActionActiveEntities`（`_actionActive` 是 `readonly List<>`）——
两者对外类型是 `IReadOnlyList<>`，没有可写索引器 / `Add` / `Clear`，字段必须在类内保持具体类型。硬转会把「缓存复用、零分配」的意图拆掉。

**另外 3 个也不是转发属性**（虽然形如 `=> _x.y`）：`CombatFrameEventTable.this[int]`、`CombatHitBuffer.this[int]`、`CombatHitBuffer.Count => _hits.Count`、`CombatBuff.Count` —— 都是计算/投影，没有 1:1 后置字段。

**结论：这类转换是纯改写，零行为变化。** 依据三条：

1. 8 个字段全是非 `readonly` 的普通字段（`readonly` 字段不能用 `private set` 表达，得用 `{ get; }`）；
2. 这 5 个文件里没有任何 `[SerializeField]`（`[SerializeField]` 全在 `Asset/` 下的资产类），不会破坏 Unity 序列化；
3. 自动属性的初始化器与字段初始化器在同一初始化阶段写编译器生成的后置字段，`= -1` / `= true` / `= DefaultVariantId` 的时机完全不变。

**行为等价的两个细节**（都在类内，`private set` 可访问）：
`_pool[Count++]`（`++` 作用在带可访问 setter 的属性上合法）、`Hp -= applied`、`InstanceId = ++LocalSequence`。

**保留的写法歧义（与用户自己的选择保持一致）**：`AttributeSet.CaptureSnapshot` 里成了 `MaxHp = MaxHp,` / `Hp = Hp,`；`CombatController.CaptureSnapshot` 里用户先前已写成 `IsGrounded = IsGrounded,`。
左侧是 `UnitAttributeSnapshot`/`UnitCombatSnapshot` 的字段，右侧是 `this` 的属性，**能编译且语义正确**（无限定名的 `MaxHp` 不可能指到结构体字段），只是读起来像自赋值。加 `this.` 可读性更好，但用户没加，所以没动。

### 两个环境发现（已写进技能）

- **本仓库不是统一 CRLF。** `AttributeSet.cs`、`CombatStageBuffers.cs` 是**纯 LF**；`CombatActionRunner.cs`、`CombatComponent.cs`、`CombatController.cs` 是 CRLF。脚本必须**按文件探测并保留**行尾，不能假定 CRLF —— 我第一版脚本就在这个断言上直接 abort 了。
- **别靠"读截断的扫描输出"数期望值。** 我把 `_presentationVariant` 的期望命中数写成 3，实际是 5：扫描报告把行截断到 90 字符，第 176/341 行末尾的传参 token 没显示出来，但它是真命中。断言前应让脚本自己给出精确计数，而不是人工从打印里数。

### 校验方法（本次用的那套）

改 `.cs` 时可以证明「中文注释一个字节没动」：把两个版本的**非 ASCII 字符单独抽出来拼成字符串**再比对。
本次改动全是 ASCII 令牌替换，所以投影必须逐字符相等 —— 5 个文件全部相等（185/479/1198/1159/710 个非 ASCII 字符）。
加上「对外成员名集合比对」（前后一致）和 `git diff --check`（输出长度 0），够替代编译验证里"没误伤"的那一半。
